server.port=8080

# DataBase
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASSNAME}
spring.datasource.hikari.maximum-pool-size=10

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Cliente WebClient
client.account.baseUrl=${CLIENT_ACCOUNT_BASEURL}
client.account.movements.path=${CLIENT_ACCOUNT_MOVEMENTS_PATH}

# Logging
logging.level.com.devsu.prueba=INFO
